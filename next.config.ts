import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  reactStrictMode: true,
  // typedRoutes: true, // Comentado temporalmente para evitar errores de tipos
};

export default nextConfig;


